[package]
name = "empires"
version = "0.1.0"
edition = "2021"
description = "Rise and Fall of Empires — a lo-fi passive world simulator for the terminal"

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "unwind"

[profile.dev]
opt-level = 1
